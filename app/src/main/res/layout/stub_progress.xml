<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="UnusedAttribute">

    <TextView
        android:id="@+id/stub_progress_content_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="@dimen/stub_progress_content_tv_layout_marginBottom"
        android:layout_marginTop="@dimen/stub_progress_content_tv_layout_marginTop"
        android:fontFamily="sans-serif"
        android:textSize="@dimen/content_textsize"
        tools:text="Message"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ProgressBar
            android:id="@android:id/progress"
            style="?android:progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="2dp"/>

        <TextView
            android:id="@+id/stub_progress_label_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@android:id/progress"
            android:layout_alignStart="@android:id/progress"
            android:layout_below="@android:id/progress"
            android:gravity="start"
            android:minWidth="@dimen/stub_progress_label_tv_minWidth"
            android:textAlignment="viewStart"
            android:textSize="@dimen/content_textsize"
            android:textStyle="bold"
            tools:text="100%"/>

        <TextView
            android:id="@+id/stub_progress_minMax_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignEnd="@android:id/progress"
            android:layout_alignRight="@android:id/progress"
            android:layout_below="@android:id/progress"
            android:gravity="end"
            android:minWidth="@dimen/stub_progress_minMax_tv_minWidth"
            android:textAlignment="viewEnd"
            android:textSize="@dimen/content_textsize"
            tools:text="100%"/>

    </RelativeLayout>

</merge>